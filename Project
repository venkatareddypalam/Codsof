git init  # Initialize Git (if not done already)
git remote add origin https://github.com/your-username/sastra-clothing.git
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
